export default function OrgDashboard() {
  // to do:
  // checks if user is authenticated
  // sees role of joined org
  // users should be able to subscribe/unsubscribe (or unjoin or join idk)

  //sort (a-z, z-a)

  //orgs must be square shaped
  //shows how many ppl in the org

  //description must be 2 liner, then trails
  return (
    <>
      <div>
        <h1>Joined Orgs</h1>

        {/* yooo gotta style this </3 */}
        <div></div>
      </div>
    </>
  );
}
